package com.isa.services.repository;

import com.isa.services.FishingLessons;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FishingLessonsRepository extends JpaRepository<FishingLessons, Long> {
}
